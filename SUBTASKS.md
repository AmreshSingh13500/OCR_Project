# SUBTASKS — Execution Tracker (Subtask Level)

**Source of truth:** [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) — descriptions below are one-line summaries only; read the plan section for full detail before implementing.
**Task tracker:** [TASKS.md](TASKS.md)
**Last updated:** 2026-07-18

## Update Rules

1. All subtasks under one task are **independent** — execute in any order, or in parallel.
2. When you start a subtask, set `Status = IN_PROGRESS`. When finished **and verified**, set `Status = DONE` and stamp the `Done on` date.
3. When the **last** subtask of a task turns `DONE`, go to [TASKS.md](TASKS.md), verify the task's AC, and roll the task status up (protocol §1 there).
4. Use `Notes` for anything the next session needs to know (deviations from plan, files touched, follow-ups). Keep it to one line; longer notes go to the Blockers & Notes Log in TASKS.md.
5. Never delete a row. Never mark `DONE` without the deliverable actually existing in the repo.

**Status values:** `PENDING` · `IN_PROGRESS` · `BLOCKED` · `DONE`

---

## T1.1 — Project scaffold (Phase 1)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T1.1.1 | Create repo layout per plan §2 + `requirements.txt` with pinned deps | DONE | 2026-07-16 | `app/__init__.py`, `app/main.py`, `app/api/__init__.py`, `app/pipeline/__init__.py`, `app/utils/__init__.py`, `requirements.txt`, `README.md`. `main.py` has no dedicated subtask owner elsewhere so its skeleton (FastAPI app + empty lifespan) lives here; T1.2.3/T1.2.4 mount routers, T3.1.1/T3.2.1 populate lifespan model loading later per Rule 3. |
| T1.1.2 | `config.py` via pydantic-settings; fail fast on missing env vars | DONE | 2026-07-16 | `app/config.py`. Required: OCR_API_KEY, LARAVEL_WEBHOOK_URL, LARAVEL_WEBHOOK_KEY, OPENAI_API_KEY (no default — verified ValidationError on missing). Others default to plan's example values. CLIP_LABELS deliberately deferred to T3.1.2. |
| T1.1.3 | `utils/logging.py` — JSON logs with case_id/message_id correlation | DONE | 2026-07-16 | `app/utils/logging.py` (JSON formatter + contextvar-based `bind_case_context`); wired into `app/main.py` lifespan via `configure_logging()`. Verified: JSON output, and no cross-task bleed under concurrent asyncio tasks. |
| T1.1.4 | `.env.example` with every var from plan §3 | DONE | 2026-07-16 | `.env.example`. Verified all 9 var names match `Settings` fields in `app/config.py` exactly (Rule 7 contract check). |

## T1.2 — Auth & API endpoints (Phase 1)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T1.2.1 | `auth.py` — Bearer token dependency, constant-time compare, 401 | DONE | 2026-07-17 | `app/auth.py` — `require_api_key()`, FastAPI dependency using `HTTPBearer(auto_error=False)` + `secrets.compare_digest()` against `settings.OCR_API_KEY`; raises `HTTPException(401)` on missing/wrong token. Verified via `tests/test_auth.py`: correct token passes, wrong token -> 401, missing token -> 401. Full request-level 401 (through the mounted endpoint) is deferred to T1.2.3's route tests. Full suite green in `.venv` (88 passed, 2 skipped, no regressions). |
| T1.2.2 | `schemas.py` — ProcessRequest / ExtractedData / WebhookPayload per PRD §4.1–4.2 | DONE | 2026-07-17 | `app/schemas.py` (new) — 3 Pydantic `BaseModel`s. `ProcessRequest`: case_id/message_id/file_url required, file_type/file_name/source_channel optional (`file_url` deliberately left an unrestricted `str` here — the https-only check is T1.2.5's 400 response, not a 422 pydantic error). `ExtractedData`: mirrors `llm_extractor.py`'s `EXTRACTED_DATA_JSON_SCHEMA` (T4.1.1) field-for-field incl. `cost` (PRD clarification #1) — kept as a separate definition from that module's hand-built OpenAI strict-mode JSON Schema (different wire format), not generated from one another, verified equal by a dedicated field-set test instead. `WebhookPayload`: mirrors `webhook_client.py`'s `build_webhook_payload()` (T4.2.1) key set exactly, `status`/`processing_path` as frozen `Literal`s (Rule 7). Also refactored `app/pipeline/orchestrator.py` to import `ProcessRequest` from here instead of keeping its own local dataclass (that duplication was flagged in orchestrator.py's own docstring as temporary pending this subtask) — Rule 7 gate checked: same 6 fields/3-required split, additive/contract-safe, no behavior change since every call site already used keyword-arg construction. New `tests/test_schemas.py` (9 tests): required/optional split, `ValidationError` on a missing required field, frozen-Literal rejection for unknown `status`/`processing_path`, and field-set equality against both `EXTRACTED_DATA_JSON_SCHEMA` and `build_webhook_payload()`. Full suite green in `.venv` (97 passed, 2 skipped, no regressions). |
| T1.2.3 | `POST /api/v1/process` — validate → BackgroundTask → 202 immediately | DONE | 2026-07-17 | `app/api/routes.py` (new) — `process_document()`: `require_api_key` (T1.2.1) dependency, body validated against `schemas.ProcessRequest` (T1.2.2, 422 on malformed body via FastAPI's own handling), `run_pipeline` (T4.3) scheduled via `BackgroundTasks.add_task()`, returns `202 {"status": "accepted", "case_id": ...}` immediately regardless of pipeline duration. `app/main.py` updated to mount the router (`app.include_router(router)`) — Rule 3 header update (added T1.2 task line, T1.2.3 subtask, HISTORY entry); import-order note re-verified n/a since orchestrator.py's own classifier-before-ocr_engine import order is unaffected. New `tests/test_routes.py` (4 tests) uses a minimal FastAPI app built in the test file (not `app.main:app`) with `run_pipeline` monkeypatched, so these tests don't trigger the real CLIP/PaddleOCR lifespan load: valid request → 202 + payload shape + `run_pipeline` scheduled with the parsed request; missing/wrong bearer token → 401; malformed body → 422. Also manually verified `app.main` imports cleanly with `/api/v1/process` mounted (no lifespan trigger). Full suite green in `.venv` (101 passed, 2 skipped, no regressions). |
| T1.2.4 | `GET /health` — 200 with clip_loaded/paddle_loaded flags, unauthenticated | DONE | 2026-07-17 | `app/api/routes.py` — `health()`, no auth dependency (monitoring probes don't carry the bearer token), reads `clip_loaded`/`paddle_loaded` from `request.app.state` (populated once at startup by `main.py`'s lifespan, T3.1.1/T3.2.1) via `getattr(..., None) is not None` so it degrades safely if hit before lifespan has run. `main.py`'s header updated (Rule 3) to note the same `include_router()` call now also mounts `/health` — no code change needed there. 2 new tests in `tests/test_routes.py`: no-auth-header request → 200 with both flags `False` (models never set on the bare test app); flags set directly on `app.state` → both `True`. Full suite green in `.venv` (103 passed, 2 skipped, no regressions). |
| T1.2.5 | Validate `file_url` is https; reject invalid URLs with 400 | DONE | 2026-07-17 | `app/api/routes.py` — `_is_valid_https_url()` (scheme == "https" and non-empty netloc via `urllib.parse.urlparse`) + a check at the top of `process_document()` raising `HTTPException(400)` before scheduling the background task. Deliberately a manual handler-level check, not a pydantic field validator on `ProcessRequest` (T1.2.2) — a validator would raise 422, but the plan requires this specific failure to be 400, distinct from the 422 a structurally malformed body already gets. 2 new tests in `tests/test_routes.py`: `http://` scheme -> 400; host-less string ("not-a-url") -> 400; both assert `run_pipeline` was never scheduled. Full suite green in `.venv` (105 passed, 2 skipped, no regressions). **T1.2 is now 5/5 — see TASKS.md for AC roll-up.** |

## T1.3 — File downloader, Step 2a (Phase 1)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T1.3.1 | `downloader.py` — async httpx GET, streaming into BytesIO, size cap mid-stream | DONE | 2026-07-16 | `app/pipeline/downloader.py` — `download_file()`. Verified with httpx.MockTransport (respx not installed locally): happy path, 404, timeout, oversize all raise/return correctly. |
| T1.3.2 | Magic-byte content detection (pdf / image / unsupported) — never trust extension | DONE | 2026-07-16 | `app/pipeline/downloader.py` — `ContentKind` enum + `detect_content_kind()`. Verified: PDF/JPEG/PNG magic bytes, GIF/text/empty → UNSUPPORTED. |
| T1.3.3 | Typed exceptions: DownloadError, FileTooLargeError, UnsupportedFileError | DONE | 2026-07-16 | `app/pipeline/downloader.py`. Done out of numeric order (before T1.3.1/T1.3.2) since both need to raise these; kept as 3 flat Exception subclasses, no shared hierarchy, to avoid except-order ambiguity in T4.3.3's mapping. |

## T2.1 — Smart PDF detection, Step 2b (Phase 2)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T2.1.1 | PyMuPDF open; password detection → PasswordProtectedError (exact PRD string) | DONE | 2026-07-16 | `app/pipeline/pdf_handler.py` — `open_pdf()`. Verified with real PyMuPDF-generated PDFs: plain PDF opens normally; AES-256-encrypted PDF raises PasswordProtectedError("Password protected document"). |
| T2.1.2 | Text extraction from first 3 pages; >100 chars → NativePdfResult | DONE | 2026-07-16 | `app/pipeline/pdf_handler.py` — `NativePdfResult` + `extract_native_text()`. Verified with real PyMuPDF PDFs: long text (170 chars) → NativePdfResult; short text and blank page → None. |
| T2.1.3 | Scanned branch: pdf2image @200 DPI, convert ≤5 pages, keep first 3 | DONE | 2026-07-16 | `app/pipeline/pdf_handler.py` — `ScannedPdfResult` + `convert_scanned_pdf()`. poppler-utils not available on this Windows dev box (Linux server dep, T2.1.4/T6.1.1) — verified page-limit logic (last_page clamp to 5, truncate to 3) via a mocked `convert_from_bytes`, matching the plan's 60-page AC exactly; module imports cleanly without poppler installed. |
| T2.1.4 | Document poppler-utils system dep in setup script + README | DONE | 2026-07-17 | `deploy/setup_server.sh` (new, minimal — just the poppler-utils install step; `deploy/setup_server.sh` is T6.1.1's primary deliverable and will extend this file per Rule 3, same pattern as `main.py` in T1.1.1) + `README.md` System requirements section. |
| T2.1.5 | Zero-page / corrupt PDFs → CorruptFileError | DONE | 2026-07-17 | `app/pipeline/pdf_handler.py` — `CorruptFileError` + zero-page check in `open_pdf()`. PyMuPDF itself refuses to save a real zero-page PDF (ValueError), so verified the page_count==0 branch via a mocked `fitz.open`; confirmed no regression on a normal 1-page PDF. |

## T2.2 — OpenCV pre-processing, Step 3 (Phase 2)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T2.2.1 | Grayscale conversion (BGR→GRAY) | DONE | 2026-07-17 | `app/pipeline/image_cleaner.py` — `to_grayscale()`. Verified shape (H,W), dtype uint8. |
| T2.2.2 | Deskew via minAreaRect + confidence gate (skip if \|angle\| < 0.5°) | DONE | 2026-07-17 | `app/pipeline/image_cleaner.py` — `deskew()` + estimators. Found/fixed two bugs vs. the classic reference snippet: (x,y) vs (row,col) coordinate order, and OpenCV ≥4.5's changed minAreaRect angle convention. Empirically validated correction formula against known rotation angles; verified 5° full-page skew → 0.0° residual, already-straight passthrough, blank-image passthrough, dtype/shape, and <2s/image performance (0.14s on A4-sized image). |
| T2.2.3 | CLAHE (clipLimit=2.0, 8×8) on grayscale before thresholding | DONE | 2026-07-17 | `app/pipeline/image_cleaner.py` — `apply_clahe()`. Verified: shape/dtype preserved, contrast (std dev) increases on a synthetic low-contrast image (8.04→21.23), high-contrast image processed without error. |
| T2.2.4 | Adaptive threshold; return both `ocr_ready` (binary) and `vision_ready` (CLAHE) | DONE | 2026-07-17 | `app/pipeline/image_cleaner.py` — `CleanedImage` + `clean_image()` (full grayscale→deskew→CLAHE→threshold pipeline). Verified on an A4-sized synthetic BGR image: shape/dtype valid, ocr_ready strictly binary (0/255), vision_ready retains grayscale detail (130 unique values, not thresholded), runs in 0.09s (<2s AC). |
| T2.2.5 | `DEBUG_SAVE_IMAGES=true` → dump each stage with case_id prefix | DONE | 2026-07-17 | `app/pipeline/image_cleaner.py` — `_debug_save_stage()` + `clean_image(image, case_id=None)`. Writes grayscale/deskewed/CLAHE/threshold stages to gitignored `debug_images/` as `{case_id}_{stage}.png`; no-op when the flag is off (verified no dir created). `case_id` param is new and optional — additive, no existing caller (orchestrator T4.3 not built yet), contract-safe per Rule 7. Verified: flag off → no dir; flag on → 4 readable PNGs with correct shape; `case_id=None` falls back to `unknown_` prefix; `CleanedImage` output unchanged. |

## T3.1 — CLIP router, Step 4a (Phase 3)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T3.1.1 | Load clip-vit-base-patch32 at startup (lifespan), CPU, no_grad | DONE | 2026-07-17 | `app/pipeline/classifier.py` — `load_clip()` called in `app/main.py` lifespan with `torch.no_grad()`. Model/processor stored in `app.state` and module-level globals via `set_clip()`. |
| T3.1.2 | Candidate labels for 4 document classes (tunable in T7.2) | DONE | 2026-07-17 | `app/config.py` — `CLIP_LABELS` list, 4 exact strings per plan §4 T3.1.2. Index 0 is the printed-document label T3.1.3 will route to Branch A; order is load-bearing for that routing check. |
| T3.1.3 | Routing rule: printed → Branch A; else Branch B; score <0.4 → Branch B fallback | DONE | 2026-07-17 | `app/pipeline/classifier.py` — `route_branch()` + `_ROUTING_CONFIDENCE_THRESHOLD=0.4`. Verified: label 0 + high confidence → Branch A; label 0 + confidence just under 0.4 → Branch B; confidence exactly 0.4 → Branch A (>=); labels 1-3 → Branch B regardless of confidence. **Update (2026-07-17, T3.2.4):** `BRANCH_A_PADDLEOCR`/`BRANCH_B_VISION` constants moved from this file to `app/config.py` — ocr_engine.py needed `BRANCH_B_VISION` and importing classifier.py (torch) triggered a real torch/paddlepaddle Windows DLL conflict; see T3.2.4's note. `route_branch()`'s behavior is unchanged, re-verified after the move. |
| T3.1.4 | Classify on `vision_ready` image (CLAHE grayscale → RGB) | DONE | 2026-07-17 | `app/pipeline/classifier.py` — `classify()`: `cv2.cvtColor(GRAY2RGB)` then CLIP zero-shot scoring against `CLIP_LABELS`, returns `(top_label_index, confidence)`. Verified end-to-end with the real downloaded model: synthetic noise image → valid index/confidence range, 0.29s (<1.5s AC); synthetic printed-text image → correctly scores label 0 (printed) at high confidence, routes to `paddleocr` via `route_branch()`. |
| T3.1.5 | Log label + confidence per page with case_id | DONE | 2026-07-17 | `app/pipeline/classifier.py` — `classify()` logs `label=... confidence=...` at INFO on every call. case_id/message_id are picked up automatically from the T1.1.3 logging contextvar, not passed as a parameter. Verified: log line shows `case_id: null` before `bind_case_context()`, and the bound values after. |

## T3.2 — PaddleOCR engine, Step 4b (Phase 3)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T3.2.1 | Init PaddleOCR (angle_cls, en, CPU) once at startup | DONE | 2026-07-17 | `app/pipeline/ocr_engine.py` — `load_paddleocr()` calls `PaddleOCR(use_angle_cls=True, lang='en', use_gpu=False)` exactly per plan; wired into `app/main.py` lifespan after CLIP loading, mirroring the `set_clip`/`get_clip` pattern. paddleocr/paddlepaddle aren't safely installable in the shared global Python env (a first attempt force-downgraded `protobuf`, breaking unrelated tooling — reverted); created an isolated project `.venv` (already gitignored) and verified for real there instead: constructor accepted the plan's exact kwargs with no deprecation errors (confirmed via ppocr's own debug namespace dump), and the full `main.py` lifespan loads both CLIP and PaddleOCR correctly in one run. |
| T3.2.2 | `extract_text(image) -> str` — reading-order line join, debug confidences | DONE | 2026-07-17 | `app/pipeline/ocr_engine.py` — `extract_text()`: runs `ocr.ocr(image, cls=True)`, sorts detected lines by box top-y explicitly (doesn't rely on PaddleOCR's internal order), joins with `\n`, logs each line's text+confidence at DEBUG. Verified in `.venv` with real PaddleOCR: text drawn in scrambled vertical order comes back correctly top-to-bottom; blank image → `""` (PaddleOCR returns `[None]` for no detections, handled explicitly). |
| T3.2.3 | Thread-safety: asyncio.Lock + run_in_executor (never block event loop) | DONE | 2026-07-17 | `app/pipeline/ocr_engine.py` — `extract_text_async()`: module-level `asyncio.Lock` serializes access to the shared PaddleOCR instance, `run_in_executor` offloads the blocking call to a thread. Verified in `.venv` with real PaddleOCR: 4 concurrent calls on 4 distinct images all returned correctly matched, non-interleaved results (`DOC 1`..`DOC 4`); a concurrent 20ms-interval heartbeat coroutine ticked 8 times during the 0.55s run, confirming the event loop was not blocked by the synchronous OCR calls. |
| T3.2.4 | Fallback: <20 chars OCR output → reroute page to vision, log reroute | DONE | 2026-07-17 | `app/pipeline/ocr_engine.py` — `should_reroute_to_vision(text)`: `_MIN_OCR_CHARS=20` threshold, logs a WARNING with char count when rerouting. Verified boundary cases (0, 1, 19, 20, long text). While implementing, found importing `classifier.py` (torch) from `ocr_engine.py` (paddlepaddle) hits a real Windows DLL conflict (paddle-before-torch import breaks torch's shm.dll load, WinError 127) — fixed by moving `BRANCH_A_PADDLEOCR`/`BRANCH_B_VISION` out of classifier.py into `app/config.py` (no ML deps), which both modules now import from instead of each other. Confirmed the underlying native conflict persists even after decoupling (paddle-loaded-anywhere-before-torch still breaks torch), but `main.py`'s existing import order (classifier before ocr_engine) avoids it — added a defensive comment there; likely Windows-only since deploy target is Ubuntu (T6.1). Re-verified full lifespan + all T3.2.1-3 functions post-refactor, no regressions. |

## T4.1 — OpenAI structured extraction, Step 5 (Phase 4)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T4.1.1 | Strict Structured Outputs JSON schema incl. `cost` (PRD clarification #1) | DONE | 2026-07-17 | `app/pipeline/llm_extractor.py` — `EXTRACTED_DATA_JSON_SCHEMA` + `RESPONSE_FORMAT`. Mirrors the ExtractedData shape (patient_name, doctor_name, diagnosis, procedure, cost, medicines), all nullable via `type: [T, "null"]` since OpenAI strict mode requires every key in `required` (no optional keys). `schemas.py` (T1.2.2) doesn't exist yet, so this is the first formal definition of the contract shape — T1.2.2 must mirror it when built. Verified structurally: property set == required set, `additionalProperties: false`, JSON-serializable, field order matches plan. `RESPONSE_FORMAT` is ready to pass unchanged to both T4.1.2 (text) and T4.1.3 (vision). |
| T4.1.2 | Text path: gpt-4o-mini chat completion with extraction system prompt | DONE | 2026-07-17 | `app/pipeline/llm_extractor.py` — `extract_from_text()` + module-level `_client`/`_EXTRACTION_SYSTEM_PROMPT`. Verified in `.venv` (pinned `openai==1.58.1`) with a mocked `chat.completions.create`: correct `model`/`response_format`/messages (exact plan system prompt + input text as user message) passed, JSON response body parsed back into the expected dict. No retry/error handling yet (T4.1.4) and no all-nulls flag (T4.1.5) — bare call by design. |
| T4.1.3 | Vision path: ≤3 base64 JPEGs (quality 85, longest side 1536 px) | DONE | 2026-07-17 | `app/pipeline/llm_extractor.py` — `extract_from_images()` + `_encode_image_base64_jpeg()`. Downscales only when the longest side exceeds 1536px (never upscales), JPEG quality 85, grayscale `vision_ready` images encoded directly (no RGB conversion needed). Truncates defensively to `MAX_PDF_PAGES_OCR` (3). Reuses T4.1.1's `RESPONSE_FORMAT` and T4.1.2's system prompt unchanged. Verified in `.venv`: 2000×1000 image → downscaled to 1536×768 (aspect preserved); 400×300 image → unchanged (no upscale); mocked `chat.completions.create` shows correct model/response_format/messages, 5 input images truncated to 3 `image_url` content items, response parsed correctly. |
| T4.1.4 | Tenacity retries: 3 attempts, exp backoff 2–30 s, retryable errors only | DONE | 2026-07-17 | `app/pipeline/llm_extractor.py` — `LLMError` + `_call_chat_completion()`/`_create_chat_completion()` (tenacity-decorated), reusing `OPENAI_MAX_RETRIES` from `app/config.py`. Retries only `APITimeoutError`/`APIConnectionError`/`RateLimitError`/`InternalServerError` with `wait_exponential(multiplier=1, min=2, max=30)`, `stop_after_attempt(3)`; 401/400 are excluded from the retry set so they propagate unwrapped immediately. `extract_from_text()`/`extract_from_images()` refactored to build a `messages` list and delegate here (T4.1.2/T4.1.3 tags kept, same primary functions). Verified in `.venv` with mocked errors: 500 ×3 → `LLMError` after exactly 3 calls; 401 → propagates as `AuthenticationError` unwrapped after exactly 1 call (no retry); 500 then success → retried once, correct parsed result. Reran T4.1.1–T4.1.3 checks — no regressions. |
| T4.1.5 | All-fields-null flag → success + informative error_message (clarification #2) | DONE | 2026-07-17 | `app/pipeline/llm_extractor.py` — `is_all_fields_null()` + `ALL_FIELDS_NULL_MESSAGE = "All fields empty - possible unreadable document"`. Rule 7 gate checked: this is a *new* frozen error string (PRD clarification #2), not an edit to the existing 7 in T4.3.3's table, so it's additive — exact wording locked from this commit on. Detection only; T4.3 (not built yet) owns pairing this with `status:"success"` in the actual webhook payload. Verified: all-null dict → True; one field filled → False; `medicines: []` (empty list, not None) → False, confirmed not conflated with null; keys match the T4.1.1 schema's 6 properties exactly. |
| T4.1.6 | Log prompt/completion token usage per call | DONE | 2026-07-17 | `app/pipeline/llm_extractor.py` — `_call_chat_completion()` logs `prompt`/`completion`/`total` token counts at INFO on every successful call, right after a call succeeds and before JSON parsing; a raised exception (retries exhausted or non-retryable) carries no `response.usage`, so nothing is logged for failed calls. Verified with a real `openai.types.completion_usage.CompletionUsage` object attached to a mocked response: exact log line `"OpenAI extraction token usage: prompt=123 completion=45 total=168"` emitted on success; confirmed no usage line is logged when a 500 exhausts retries and raises `LLMError`. This was T4.1's last subtask — all 6/6 done. |

## T4.2 — Laravel webhook return, Step 6 (Phase 4)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T4.2.1 | `webhook_client.py` — async POST with Bearer key, WebhookPayload body | DONE | 2026-07-17 | `app/pipeline/webhook_client.py` — `build_webhook_payload()` (exact WebhookPayload key set, first formal definition since schemas.py/T1.2.2 doesn't exist yet, same precedent as T4.1.1) + `send_webhook()` (bare async httpx POST, Bearer `LARAVEL_WEBHOOK_KEY` header, no retry/error handling yet — T4.2.2/T4.2.3). Also added `tests/conftest.py` (new, minimal — sets the 4 required env vars so `Settings()` can instantiate under pytest; not the full plan-§2 conftest.py with sample-file/mock fixtures, which is T5.1.1's deliverable and will extend this file per Rule 3). Verified via committed respx test `tests/test_webhook_client.py`: payload key-set exact match, POST hits `LARAVEL_WEBHOOK_URL` with correct Bearer header and JSON body — both tests green in `.venv`. |
| T4.2.2 | Retries on 5xx/connection only (3×, exp backoff); 4xx = no retry, CRITICAL log | DONE | 2026-07-17 | `app/pipeline/webhook_client.py` — `_RetryableWebhookError` + `_post_webhook_with_retry()` (tenacity: retries only 5xx/connection failures, 3 attempts, exp backoff 2-30s, mirrors T4.1.4's shape) + `send_webhook()` now catches the unwrapped 4xx `HTTPStatusError` and logs CRITICAL immediately (no retry). Verified via `tests/test_webhook_client.py`: 503 then 200 → 2 calls, no CRITICAL log; 401 → exactly 1 call + 1 CRITICAL log mentioning the status code — both green in `.venv` (4/4 tests passing, real exponential backoff elapsed ~2s). |
| T4.2.3 | Retries exhausted → CRITICAL log with full replayable payload JSON | DONE | 2026-07-17 | `app/pipeline/webhook_client.py` — `send_webhook()` now also catches `_RetryableWebhookError` (retries exhausted) and logs CRITICAL with the full payload JSON (`json.dumps(payload)`) for manual replay; delivery failure is fully terminal here (no persistence/dead-letter queue in Phase 1, plan §8-3) and `send_webhook()` never raises to its caller either way. Verified via `tests/test_webhook_client.py::test_send_webhook_5xx_retries_exhausted_logs_critical_with_payload`: 4 queued 503/502/500/500 responses, only 3 consumed (stop_after_attempt=3), exactly 1 CRITICAL log containing the exact payload JSON. T4.2 AC fully covered: all 4 respx scenarios (success, 5xx→retry→success, 5xx×4→CRITICAL+payload, 401→single attempt+CRITICAL) green — 5/5 tests passing in `.venv`. |

## T4.3 — Pipeline orchestrator (Phase 4)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T4.3.1 | `run_pipeline()` wiring Steps 2→6; direct images skip Step 2b | DONE | 2026-07-17 | `app/pipeline/orchestrator.py` — `ProcessRequest` (first formal PRD §4.1 definition, schemas.py/T1.2.2 doesn't exist yet, same precedent as T4.1.1/T4.2.1) + `run_pipeline()` + `_extract_from_pages()` helper. Wires download→detect kind→native-PDF text (skips straight to Step 5) / scanned-PDF pages / direct-image passthrough (skips Step 2b)→clean→classify→OCR-or-vision→LLM→webhook. Mixed-page rule implemented pragmatically (any page needing vision → whole call uses vision with every page's image) since extract_from_text/extract_from_images can't be combined in one call; T4.3.2 owns formally verifying/tagging that rule. No error mapping (T4.3.3) or guaranteed-one-webhook try/except (T4.3.4) yet — bare wiring, same "error handling comes later" pattern as T4.1.2/T4.2.1. Imports classifier (torch) before ocr_engine (paddlepaddle) — same defensive order as main.py (T3.2.4 DLL note). Verified via new `tests/test_pipeline_e2e.py` (4 tests, all collaborators monkeypatched): native PDF → `native_pdf` + extract_from_text, no cleaning/classify called; scanned printed page → `paddleocr` + extract_from_text on OCR output; direct image (handwritten) → `vision_api` + extract_from_images, Step 2b skipped; unsupported content → `UnsupportedFileError` raised. Full suite green in `.venv` (9/9, no regressions, no torch/paddle DLL conflict).
| T4.3.2 | Set processing_path (native_pdf / paddleocr / vision_api; vision wins mixed) | DONE | 2026-07-17 | `app/pipeline/orchestrator.py` — tagged the processing_path decision inside `_extract_from_pages()` (already built as part of T4.3.1's necessary wiring): `paddleocr` only when every page's OCR output was trusted, `vision_api` wins the instant any single page needs vision (originally routed there, or rerouted via T3.2.4's low-yield check) — all pages' images sent together in that case since extract_from_text/extract_from_images can't be combined in one call. No behavior change, formal tagging + verification only. Verified via 3 new tests in `tests/test_pipeline_e2e.py`: mixed pages (one paddleocr + one vision-routed) → `vision_api`, both pages' images sent, extract_from_text never called; mixed via low-OCR-yield reroute (page 2 <20 chars) → `vision_api`; all-paddleocr 2-page set → stays `paddleocr` with concatenated text. Full suite green in `.venv` (12/12, no regressions).
| T4.3.3 | Error mapping table — 7 exception → error_message strings exactly per plan | DONE | 2026-07-17 | `app/pipeline/orchestrator.py` — `_ERROR_MESSAGES` dict + `map_exception_to_error_message()`. Flat dict keyed by exact exception type (not isinstance) per T1.3.3's precedent (those exceptions were deliberately kept as flat classes with no shared hierarchy for this exact reason); covers all 6 named exceptions (PasswordProtectedError, DownloadError, FileTooLargeError, UnsupportedFileError, CorruptFileError, LLMError) plus the "any uncaught Exception" fallback → "Internal processing error" with full traceback logged via `exc_info`. Pure mapping function only — not wired into a try/except yet (T4.3.4's job). Rule 7 gate checked: reproduces the plan's 7 already-frozen strings verbatim, none invented here — additive/contract-safe. Verified via 8 new tests in `tests/test_pipeline_e2e.py` (parametrized over all 7 rows + a dedicated traceback-logging assertion for the fallback). Full suite green in `.venv` (20/20, no regressions).
| T4.3.4 | Top-level try/except: every accepted request → exactly one webhook call | DONE | 2026-07-17 | `app/pipeline/orchestrator.py` — extracted Steps 2-5 out of `run_pipeline()` into a new `_run_extraction()` (T4.3.1's tag moves with it, no behavior change), so `run_pipeline()` itself is now a try/except/else: success builds a `status:"success"` payload from `_run_extraction()`'s result; any exception is mapped via T4.3.3's `map_exception_to_error_message()` into a `status:"error"`/`processing_path:None`/`extracted_data:None` payload (with a WARNING log naming the mapped error_message). `send_webhook()` is called exactly once either way, after the try/except/else resolves — no code path calls it zero or twice, and `send_webhook()` itself never raises (T4.2.2/T4.2.3's CRITICAL-log contract) so this is sufficient for the AC. Verified via 3 new tests in `tests/test_pipeline_e2e.py`: UnsupportedFileError (previously asserted a raise; now asserts exactly one error webhook), a mapped exception (PasswordProtectedError) → exactly one error webhook with the frozen message, and an unmapped exception (RuntimeError) → exactly one error webhook with "Internal processing error" + traceback logged. Full suite green in `.venv` (22/22, no regressions). T4.3.5 (timing log) is the only remaining subtask before T4.3's AC can be rolled up.
| T4.3.5 | Per-request timing log: total + per-step milliseconds | DONE | 2026-07-17 | `app/pipeline/orchestrator.py` — `_StepTimings` (accumulates elapsed ms per named step, summing repeat samples across a multi-page request's Steps 3-4 rather than overwriting) + `_timed()` context manager (records elapsed time on exit even if the timed block raises, so a failed request still gets partial per-step timing). Threaded `timings` through `_run_extraction()`/`_extract_from_pages()`, wrapping `step2a_download`, `step2b_pdf_detect`, `step3_clean`, `step4a_classify`, `step4b_ocr`, `step5_llm`, `step6_webhook`. `run_pipeline()` logs one `"Pipeline request timing: total_ms=... steps_ms=..."` INFO line per request on both the success and error paths. Logging-only addition — no change to any return value or webhook payload shape. Verified via 4 new tests in `tests/test_pipeline_e2e.py`: `_StepTimings` accumulation across repeated steps, `_timed()` recording elapsed time through a raised exception, and `run_pipeline()` emitting exactly one timing line with the expected step keys on both a success run and an error run. Full suite green in `.venv` (26/26, no regressions). **T4.3 is now 5/5 — rolled up to DONE, see TASKS.md.**

## T5.1 — Test suite completion (Phase 5)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T5.1.1 | Assemble 7 test fixtures — synthetic/anonymized only, no real patient data | DONE | 2026-07-17 | `tests/fixtures/generate_fixtures.py` (new, one-shot generator) + `tests/fixtures/ground_truth.py` (fabricated document text + expected ExtractedData per fixture) + the 7 committed binary files: native.pdf (real PyMuPDF text layer, 199 chars > NATIVE_PDF_MIN_CHARS), scanned.pdf (4 image-only pages, no text layer, JPEG-embedded to keep the file ~65KB), printed_report.jpg/handwritten.jpg (Arial vs. cursive Segoe Script system fonts), medicine_box.jpg (drawn box + blister pack, not a photo), password.pdf (AES-256 encrypted, `needs_pass=True` verified), blurry.jpg (printed_report.jpg + heavy Gaussian blur, for the all-fields-null/unreadable scenario). All content fabricated (John Smith/Maria Garcia/etc.) — no real patient data. Verified: native.pdf text extraction (199 chars), password.pdf needs_pass=1, scanned.pdf 4 pages/0 text chars — all via real PyMuPDF in `.venv`. Generator script hardcodes local Windows font paths (dev-only tooling, not part of the app/ runtime pipeline) with a PIL-default-font fallback if absent. |
| T5.1.2 | Coverage ≥80% on app/pipeline/; all Phase 1–4 AC tests green | DONE | 2026-07-17 | Backfilled the committed pytest files the plan's §2 layout lists but that Phases 1-4 had only verified ad hoc (per their own SUBTASKS.md notes): `tests/test_downloader.py` (T1.3, respx-mocked), `tests/test_pdf_handler.py` (T2.1, real native.pdf/password.pdf fixtures; scanned.pdf conversion skipped — no poppler on this box), `tests/test_image_cleaner.py` (T2.2, real printed_report.jpg), `tests/test_classifier.py` (T3.1, real CLIP model against real printed_report.jpg/handwritten.jpg/medicine_box.jpg — **resolves the 2026-07-17 T3.1 AC blocker note**: handwritten.jpg's first (crisp cursive-font) rendering actually scored 0.42 printed vs. 0.58 handwritten only after T5.1.1's fixture was redesigned with ruled paper, baseline jitter, page rotation, and grain/blur; now correctly routes to Branch B), `tests/test_ocr_engine.py` (T3.2, real PaddleOCR against real printed_report.jpg), `tests/test_llm_extractor.py` (T4.1, mocked `openai.OpenAI` client — the live native.pdf smoke test AC item is written but gated behind `OPENAI_LIVE_SMOKE_TEST=1` + a real key, neither available here, per the 2026-07-17 T4.1 AC blocker note, still open). Added `pytest-cov==7.1.0` to requirements.txt. Also pinned a defensive `import app.pipeline.classifier` at the top of `tests/conftest.py` so torch always loads before paddlepaddle process-wide (the T3.2.4 Windows DLL conflict), instead of relying on alphabetical test-file collection order. Coverage: `pytest --cov=app.pipeline` → **96%** (414 stmts, 17 missed; every individual file ≥86%), 75 passed + 2 correctly-skipped (poppler / live-OpenAI-key gates) in `.venv`. `test_auth.py` intentionally not created — T1.2 (`auth.py`) doesn't exist yet (see TASKS.md §5, T5.1 dependency note). |
| T5.1.3 | Concurrency test: 8 parallel requests → 8 webhooks, no data bleed | DONE | 2026-07-17 | `tests/test_concurrency.py` (new) — 8 concurrent `run_pipeline()` calls (T1.2's HTTP endpoint doesn't exist yet, see TASKS.md §5 T5.1 dependency note, so this exercises the exact unit `POST /api/v1/process`'s BackgroundTask will dispatch once T1.2 lands), with a staggered fake download step so the 8 tasks genuinely interleave rather than completing in submission order. Verified: exactly 8 webhook calls, one case_id each (no dupes/drops), and each payload's `extracted_data` — tagged from the case-context contextvar (T1.1.3) inside the monkeypatched `extract_from_text` — matches its own case_id, never a concurrent request's (the literal "no data bleed" AC). 1/1 passed in `.venv`. |
| T5.1.4 | Accuracy validation harness (field-level accuracy report script) | DONE | 2026-07-17 | `scripts/accuracy_harness.py` (new) — runs each T5.1.1 ground-truthed fixture through the real Steps 2b-5 pipeline (reusing `orchestrator._extract_from_pages`/`_pil_to_bgr`, sourcing bytes from disk instead of a download), scores the 6 ExtractedData fields against `tests/fixtures/ground_truth.py`, and reports per-bucket accuracy ("printed" = native_pdf/paddleocr, "handwritten" = vision_api) against the plan's >95%/>85% targets; password.pdf/blurry.jpg are scored as pass/fail special cases (must raise `PasswordProtectedError` / must come back all-null) since they have no field ground truth. `tests/test_accuracy_harness.py` (new) unit-tests the pure scoring/report logic and an end-to-end `run_all_fixtures()` pass with the real extraction step stubbed to a perfect match — proves the harness produces a correct report (the plan's literal AC) without real models/API calls. A real run needs a real `OPENAI_API_KEY` + poppler-utils (neither available in this dev environment) — documented in README's new "Accuracy harness" section; T7.2.1 will run it for real against ≥30 real anonymized documents. Also fixed a pre-existing flaky test found while building this: `test_classifier.py`'s <1.5s inference AC intermittently failed when run as part of the full suite (cold-start cost attributed to whichever test ran the first `classify()` call) — added a warmup call in the module fixture; full suite green across repeated runs after the fix. |

## T5.2 — Security hardening (Phase 5)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T5.2.1 | Verify constant-time token compare; no tokens in logs | DONE | 2026-07-17 | `app/auth.py` — re-verified `require_api_key()` (T1.2.1) still uses `secrets.compare_digest` and has no logging statement at all, so neither the correct `OCR_API_KEY` nor a caller-supplied token can leak. New `tests/test_security_hardening.py` (4 tests): wrong/missing/valid-token requests through the real mounted `/api/v1/process` endpoint, captured at DEBUG level, assert the real token and the wrong attempted token never appear in any log record; a source-inspection test locks in `compare_digest` usage as a regression guard. Full suite green in `.venv` (109 passed, 2 skipped, no regressions). |
| T5.2.2 | Privacy logging: field values DEBUG-only; presence booleans at INFO | DONE | 2026-07-17 | `app/pipeline/orchestrator.py` — new `_log_extracted_data_privacy_safe()` called from `run_pipeline()`'s success branch: INFO log with per-field presence booleans, DEBUG log with actual values. `app/pipeline/webhook_client.py` — new `_redact_extracted_data()`; both CRITICAL failure-log call sites in `send_webhook()` now log a redacted payload (presence booleans, not raw values) plus a new DEBUG-level log with the full unredacted payload (still needed for manual replay per T4.2.3, just moved to DEBUG). Rule 7 gate checked: only local log output changed, not the actual `WebhookPayload` dict POSTed to Laravel (unaffected) nor any frozen error string — contract-safe. 3 new tests in `tests/test_security_hardening.py`: presence-vs-value log-level split (direct unit test of the orchestrator helper), `_redact_extracted_data()` unit test, and an end-to-end `send_webhook()` 401 case asserting the CRITICAL log has no raw value while the DEBUG log has the full JSON payload. Full suite green in `.venv` (112 passed, 2 skipped, no regressions). |
| T5.2.3 | Request body size limit (Nginx 1 MB + FastAPI level) | DONE | 2026-07-17 | `app/config.py` — new `MAX_REQUEST_BODY_BYTES = 1 MB` fixed constant. `app/api/routes.py` — new `enforce_body_size_limit()` dependency (checks `Content-Length` first for a fast rejection, falls back to the actual buffered `request.body()` length — Starlette caches it, so no double-read — so an understated/missing Content-Length can't bypass the cap), wired into `POST /api/v1/process` alongside `require_api_key`. Deliberately app-level only here — the Nginx half (`deploy/nginx.conf`'s `client_max_body_size 1m`) is T6.2.2's deliverable, not yet built (Phase 6); this subtask's scope is the FastAPI-level half of the plan's "Nginx 1 MB + FastAPI level" pair. 5 new tests in `tests/test_security_hardening.py`: oversized body -> 413 (run_pipeline never scheduled), normal body unaffected, Content-Length-lies-small-but-real-body-is-large still caught, and an oversized-Content-Length fast path that never even calls `body()`. Full suite green in `.venv` (116 passed, 2 skipped, no regressions). |
| T5.2.4 | SSRF guard: host allowlist env + reject private/loopback IPs post-DNS | DONE | 2026-07-17 | `app/config.py` — new `ALLOWED_FILE_HOSTS: Optional[str] = None` setting (also added to `.env.example` and `IMPLEMENTATION_PLAN.md` §3's env var table for consistency). `app/api/routes.py` — `_host_matches_allowlist()` (exact/`*.domain.com` wildcard matching), `_is_disallowed_ip()` (private/loopback/link-local/reserved/multicast/unspecified via `ipaddress`), `_resolve_and_check_host()` (async `loop.getaddrinfo` DNS resolution, IP-literal hosts checked directly with no DNS call, unresolvable = unsafe), `_is_safe_file_url()` (combines the optional allowlist with the always-on IP check), called from `process_document()` right after the T1.2.5 https check, 400 on failure. Deliberately request-time rejection (not deep in the download step) matching T1.2.5's pattern. Explicitly out of scope: DNS-rebinding protection between this check and Step 2a's separate download connection — noted in the module docstring as a known limitation of this app-level hardening pass, not a full egress-proxy solution. 20 new tests in `tests/test_security_hardening.py`: allowlist matching, IP-range classification, DNS-mocked hostname-resolution (public/private/unresolvable, no real network dependency), and end-to-end 400s for `169.254.169.254`, `127.0.0.1`, `localhost` through a new `ssrf_client` fixture that (unlike the shared `client` fixture) doesn't bypass the real check; a public-IP-literal request confirms the guard doesn't over-block. `tests/test_routes.py` and this file's own `client` fixture both bypass `_is_safe_file_url` for their non-SSRF tests (documented in each file's header) so `"https://x/doc.pdf"` keeps working as a placeholder without a real DNS lookup. **AC deviation** (same precedent as T3.1/T4.1/T1.2's AC notes, see TASKS.md §5): the plan's literal AC examples use `http://169.254.169.254/...` and `http://localhost/...`, but T1.2.5 already requires `https://` — those exact URLs would be rejected as non-https before ever reaching this guard. Tested the equivalent `https://` scheme instead (`https://169.254.169.254/...`, `https://localhost/...`) to actually exercise this guard's code path; the IP-safety property under test is identical regardless of scheme. Full suite green in `.venv` (132 passed, 2 skipped, no regressions). **T5.2 is now 4/4 — see TASKS.md for AC roll-up.** |

## T6.1 — Server provisioning script (Phase 6)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T6.1.1 | `setup_server.sh` — apt deps, service user, venv, pip install | DONE | 2026-07-18 | `deploy/setup_server.sh` (extended in place per Rule 3 from T2.1.4's poppler-only stub). Full AlmaLinux 9 provisioning: EPEL+CRB enable, `dnf install` python3.12/poppler-utils/nginx/mesa-libGL+glib2/certbot+nginx-plugin, persistent SELinux `httpd_can_network_connect` (for T6.2 nginx→gunicorn proxy), `ocrsvc` system user (nologin, home=/opt/ocr-service), and a service-owned venv built with `python3.12 -m venv` + `pip install -r requirements.txt`. **Plan deviation:** plan says Ubuntu/apt/UFW/libgl1 but TASKS.md §5 (2026-07-17) records the AlmaLinux 9 decision that supersedes it → dnf/firewalld/python3.12/mesa-libGL used; poppler-utils T2.1.4 line migrated apt→dnf (ID kept, HISTORY appended). Root-required + idempotent (id/user guards, `install -d`). Verified `bash -n` clean (shellcheck unavailable on this Windows box); full-VM unattended-run AC is owned by T6.3 (no pytest surface for a provisioning script). Rule 7 n/a (no contract surface touched). |
| T6.1.2 | UFW: deny incoming, allow 22+443 only; certbot port-80 renewal hooks | DONE | 2026-07-18 | `deploy/setup_server.sh` (T6.1.2 section appended per Rule 3). **firewalld, not UFW** (TASKS.md §5 AlmaLinux decision #4): `systemctl enable --now firewalld`, public zone default-deny, `--add-port=22/tcp`+`443/tcp`, and removes AlmaLinux's pre-seeded services (ssh/cockpit/dhcpv6-client) so `--list-ports` reads exactly "22/tcp 443/tcp" (matches the plan's literal AC). Port 80 stays closed except during renewal: writes global certbot hooks `renewal-hooks/pre/open-port-80.sh` (runtime `--add-port=80/tcp`) + `post/close-port-80.sh` (runtime `--remove-port=80/tcp`, no `--permanent` so a reboot can't leave 80 open), uses the `--standalone` HTTP-01 window called for in plan T6.1.2, and enables `certbot-renew.timer`. Verified `bash -n` clean incl. the two nested heredocs; live-VM `firewall-cmd` AC owned by T6.3. Rule 7 n/a. |
| T6.1.3 | Model warmup script — pre-download CLIP + PaddleOCR weights | DONE | 2026-07-18 | `scripts/warmup_models.py` (new, mirrors the `scripts/accuracy_harness.py` tooling pattern — run via `python -m scripts.warmup_models`) + `tests/test_warmup_models.py` (new). `warmup()` reuses the app's own `load_clip()`/`load_paddleocr()` so the exact model IDs/args production loads are what gets cached (no drift). Imports classifier (torch) before ocr_engine (paddle) per the T3.2.4/main.py order; sets placeholder env vars via `os.environ.setdefault` so `app.config.Settings()` instantiates during provisioning before `/etc/ocr-service/env` exists (loaders never read those secrets; a real env is left intact). `setup_server.sh` invokes it as `ocrsvc` with `HOME=/opt/ocr-service` + cwd=repo-root at the end of provisioning (T6.1.3 body block). Test monkeypatches both loaders (no real ~600 MB download) and asserts load_clip→load_paddleocr called once each in order, returns 2. Verified: `bash -n` clean; new test green + **full suite 133 passed / 2 skipped in `.venv`, no regressions**. Real weight-caching + first-request speedup validated on the deploy server (T6.3). Rule 7 n/a. **T6.1 is now 3/3 — see TASKS.md for AC roll-up.** |

## T6.2 — Process & proxy configuration (Phase 6)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T6.2.1 | systemd unit: gunicorn 4 uvicorn workers, Restart=always, EnvironmentFile | DONE | 2026-07-18 | `deploy/ocr-service.service` (new). Runs `/opt/ocr-service/.venv/bin/gunicorn app.main:app` with `--workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 127.0.0.1:8000 --timeout 300` (plan T6.2.1 verbatim), as `ocrsvc`/`ocrsvc` with `WorkingDirectory=/opt/ocr-service` — the exact user/venv/APP_DIR provisioned in `setup_server.sh` (T6.1.1). `EnvironmentFile=/etc/ocr-service/env` (deploy-time file, not in repo — no secrets in the unit). `Restart=always`+`RestartSec=3`+`WantedBy=multi-user.target` cover the "survives reboot/crash" AC. Two justified additions beyond the plan's literal list: `Type=notify` (gunicorn's native sd_notify → accurate systemd readiness) and `TimeoutStartSec=300` (4 workers each loading CLIP+PaddleOCR can exceed systemd's default 90 s window even with T6.1.3 warmup). Bound to loopback only — nginx (T6.2.2) is the sole public entry. No pytest surface (config file); structure verified by inspection, live `systemctl status`/reboot AC owned by T6.3. Rule 7 n/a (no contract surface). |
| T6.2.2 | nginx.conf: 443 SSL, proxy_pass, 1 MB body, 300 s read timeout, headers | PENDING | — | — |
| T6.2.3 | Document 4–6 GB RAM budget + 2-worker fallback for 8 GB boxes in README | PENDING | — | — |

## T6.3 — E2E deployment validation (Phase 6)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T6.3.1 | External smoke test: real docs via public HTTPS → mock-Laravel webhook | PENDING | — | — |
| T6.3.2 | Load test: 10 concurrent docs; 202 <500 ms, all webhooks, RAM stable | PENDING | — | — |
| T6.3.3 | Log rotation configured (journald limits or logrotate) | PENDING | — | — |

## T7.1 — Contract integration test with Laravel (Phase 7)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T7.1.1 | Exchange final bearer tokens both directions (secure channel) | PENDING | — | — |
| T7.1.2 | Joint test: WhatsApp → Laravel → Python → webhook → Case History | PENDING | — | — |
| T7.1.3 | Verify Laravel handles all 7 error_message strings | PENDING | — | — |

## T7.2 — Accuracy tuning pass (Phase 7)

| ID | Summary | Status | Done on | Notes |
|---|---|---|---|---|
| T7.2.1 | Run accuracy harness on ≥30 real anonymized docs; measure per path | PENDING | — | — |
| T7.2.2 | Tune CLIP labels/threshold, threshold params, deskew gate, LLM prompt | PENDING | — | — |
| T7.2.3 | Record final accuracy vs PRD targets in README (or mitigation plan) | PENDING | — | — |
